// String, number, undefined, null, boolean
const nome = 'Pedro'; // String
const nome1 = "Pedro"; // String
const nome2 = `Pedro`; //Strings
const num1 = 10; // number
const num2 = 10.52; // number
let nomeAluno; //undefined -> não aposta pra local nenhum na memoria
const sobrenomeAluno = null; //não aposta pra local nenhum na memoria
const aprovado = false; // Boolean = true ou false (lógico)

let a = 2;
const b = a;
console.log(a, b); // 2, 2

a = 3;
console.log(a, b); // 3, 2